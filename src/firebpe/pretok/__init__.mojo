"""Pre-tokenizer package.

Default export is ScannerPreTok (pure Mojo).
PyRegexPreTok is available but requires the pyregex pixi environment
and must be explicitly imported.
"""

from .scanner import ScannerPreTok
